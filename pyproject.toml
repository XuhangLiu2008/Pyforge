[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "pyforge-local"
version = "0.0.0"
description = "Local Pyforge utilities"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["filament_sample"]
py-modules = ["filament"]
